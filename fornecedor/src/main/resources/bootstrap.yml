
spring:
  application:
    name: 'fornecedor'
  profiles:
    active: default
  cloud:
    config:
      uri: http://localhost:8888



      #Arquivo lido em um contexto anterior ao contexto do application.yml para o banco
      #carregar quando a aplicação já tiver essas infos